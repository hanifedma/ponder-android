<?xml version="1.0" encoding="utf-8"?>
<!--
  What the widget picker shows (Android 12+). It is never inflated as a real
  widget, so unlike widget_thought.xml it carries its own sample content — a
  live widget at this point would have no quote to show and would preview as an
  empty card.

  Deliberately the dark card: it is the app's default theme, and the one the
  artwork and launcher icon are matched to.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg_dark"
    android:padding="14dp"
    tools:ignore="HardcodedText">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:baselineAligned="false">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="11.5sp"
            android:textColor="#A6A6A6"
            android:includeFontPadding="false"
            android:text="❝ Ponder" />

        <ImageView
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_marginStart="6dp"
            android:padding="6dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_widget_shuffle"
            android:background="@drawable/widget_pill_dark"
            android:contentDescription="@string/widget_next" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="5dp"
        android:gravity="top|start"
        android:ellipsize="end"
        android:maxLines="4"
        android:textSize="16sp"
        android:textColor="#F0F0F0"
        android:lineSpacingMultiplier="1.15"
        android:text="The two most powerful warriors are patience and time." />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="11.5sp"
        android:textColor="#6F6F6F"
        android:includeFontPadding="false"
        android:text="— Leo Tolstoy" />
</LinearLayout>
